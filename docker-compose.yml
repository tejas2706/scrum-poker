services:
  server:
    build:
      context: ./server
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
    volumes:
      - ./server/data:/app/data

  client:
    build:
      context: ./client
    depends_on:
      - server
    ports:
      - "5173:80"
